{
  "name": "dino-game",
  "version": "1.0.0",
  "description": "Trex dinosur game terminal implementation",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node ./src/index.ts",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "trex",
    "dinosur",
    "game",
    "chromium",
    "node"
  ],
  "author": "wassimoo",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.0",
    "ts-node": "^8.4.1",
    "tslint": "^5.18.0",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "ansi-escapes": "^4.2.1",
    "keypress": "^0.2.1"
  }
}
